using UnityEngine;
using System.Collections;

public class FieldBuilder : MonoBehaviour
{
    // Use this for initialization
    private void Start()
    {
//        this.transform.localScale *= new Vector3(10, 0);
    }

    // Update is called once per frame
    private void Update()
    {
    }
}